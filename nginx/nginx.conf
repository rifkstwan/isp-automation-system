server {
    listen 80;
    server_name localhost;

    # Root Laravel public directory
    root /var/www/html/public;
    index index.php index.html;

    # Resolver internal DNS Docker (127.0.0.11) agar Nginx selalu update IP backend jika backend restart
    resolver 127.0.0.11 valid=5s;

    # ─────────────── Logging ───────────────
    access_log /var/log/nginx/access.log;
    error_log  /var/log/nginx/error.log;

    # ─────────────── Upload size ───────────────
    client_max_body_size 50M;

    # ─────────────── Laravel Routing ───────────────
    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    # ─────────────── PHP-FPM (Backend Laravel) ───────────────
    location ~ \.php$ {
        set $upstream_backend backend:9000;
        fastcgi_pass $upstream_backend;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        fastcgi_param DOCUMENT_ROOT    $realpath_root;
        include fastcgi_params;

        fastcgi_read_timeout 300;
        fastcgi_buffer_size 16k;
        fastcgi_buffers 4 16k;
    }

    # ─────────────── Security: sembunyikan .env dan file sensitif ───────────────
    location ~ /\. {
        deny all;
    }

    location ~ \.(env|log|sh)$ {
        deny all;
    }
}
